## 混排与链接

这是一段中英混排示例：recent work on **style transfer** 和 **image editing** 常常共用同一套表征。随便放个链接 [Python-Markdown](https://python-markdown.github.io/) 看看样式。

> 引用块：Journal 只是随笔区，不必像论文那样严谨——但标点还是尽量对齐比较好。

下面是一段行内 `code` 和代码块：

```python
def hello():
    return "preview"
```
